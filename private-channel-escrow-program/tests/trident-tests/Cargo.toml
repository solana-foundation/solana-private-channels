[workspace]

[package]
name = "fuzz_tests"
version = "0.1.0"
description = "Created with Trident"
edition = "2021"

[dependencies]
solana-sdk = "2.3.1"
spl-associated-token-account = { version = "4.0.0", features = ["no-entrypoint"] }
private-channel-escrow-program-client = { path = "../../clients/rust" }

# Trident is pinned to solana-sdk 2.x while the client is on solana-pubkey 4 /
# solana-instruction 3. Named directly so `shared.rs` can convert between them.
solana-pubkey = "4"
solana-instruction = "3"

[dependencies.trident-fuzz]
version = "0.12.0"
features = ["token"]

# Patch Trident to work with the syscalls necessary for Pinocchio
[patch.crates-io]
trident-svm = { git = "https://github.com/exo-tech-xyz/trident-svm", branch = "fix-syscalls" }

[[bin]]
name = "fuzz_escrow"
path = "fuzz_escrow.rs"

[[bin]]
name = "fuzz_rotate_bitmap"
path = "fuzz_rotate_bitmap.rs"
