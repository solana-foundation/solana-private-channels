apiVersion: 1
policies:
  - orgId: 1
    receiver: default
    # Batch related alerts and avoid re-nagging too often. Tune per deployment.
    group_by: ['alertname']
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
