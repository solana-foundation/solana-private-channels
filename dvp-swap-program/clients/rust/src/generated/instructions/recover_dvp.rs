//! This code was AUTOGENERATED using the codama library.
//! Please DO NOT EDIT THIS FILE, instead use visitors
//! to add features, then rerun codama to update it.
//!
//! <https://github.com/codama-idl/codama>
//!

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use solana_pubkey::Pubkey;

pub const RECOVER_DVP_DISCRIMINATOR: u8 = 5;

/// Accounts.
#[derive(Debug)]
pub struct RecoverDvp {
    /// Depositor of the leg being recovered; must equal the leg's user; receives the closed escrow's rent
    pub signer: solana_pubkey::Pubkey,
    /// Closed SwapDvp address, re-derived from the seed args; must be system-owned and empty (signs the CPIs)
    pub swap_dvp: solana_pubkey::Pubkey,
    /// Nonce tombstone PDA for swap_dvp; must be program-owned
    pub nonce_tombstone: solana_pubkey::Pubkey,
    /// Mint of the leg being recovered; mint_a if signed by user_a, mint_b if by user_b
    pub mint: solana_pubkey::Pubkey,
    /// Recreated escrow ATA for the closed DvP (drained, then closed)
    pub dvp_escrow_ata: solana_pubkey::Pubkey,
    /// Signer's canonical ATA for the leg's mint
    pub signer_dest_ata: solana_pubkey::Pubkey,
    /// SPL Token or Token-2022 program; must own dvp_escrow_ata (the program the escrow was created under, regardless of the mint's current owner)
    pub token_program: solana_pubkey::Pubkey,
    /// SPL Memo program; only used if signer_dest_ata requires a memo
    pub memo_program: solana_pubkey::Pubkey,
}

impl RecoverDvp {
    pub fn instruction(&self, args: RecoverDvpInstructionArgs) -> solana_instruction::Instruction {
        self.instruction_with_remaining_accounts(args, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::vec_init_then_push)]
    pub fn instruction_with_remaining_accounts(
        &self,
        args: RecoverDvpInstructionArgs,
        remaining_accounts: &[solana_instruction::AccountMeta],
    ) -> solana_instruction::Instruction {
        let mut accounts = Vec::with_capacity(8 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(self.signer, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.swap_dvp,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.nonce_tombstone,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.mint, false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.dvp_escrow_ata,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            self.signer_dest_ata,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.token_program,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            self.memo_program,
            false,
        ));
        accounts.extend_from_slice(remaining_accounts);
        let mut data = RecoverDvpInstructionData::new().try_to_vec().unwrap();
        let mut args = args.try_to_vec().unwrap();
        data.append(&mut args);

        solana_instruction::Instruction {
            program_id: crate::DVP_SWAP_PROGRAM_ID,
            accounts,
            data,
        }
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RecoverDvpInstructionData {
    discriminator: u8,
}

impl RecoverDvpInstructionData {
    pub fn new() -> Self {
        Self { discriminator: 5 }
    }

    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

impl Default for RecoverDvpInstructionData {
    fn default() -> Self {
        Self::new()
    }
}

#[derive(BorshSerialize, BorshDeserialize, Clone, Debug, Eq, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct RecoverDvpInstructionArgs {
    pub settlement_authority: Pubkey,
    pub user_a: Pubkey,
    pub user_b: Pubkey,
    pub mint_a: Pubkey,
    pub mint_b: Pubkey,
    pub nonce: u64,
}

impl RecoverDvpInstructionArgs {
    pub(crate) fn try_to_vec(&self) -> Result<Vec<u8>, std::io::Error> {
        borsh::to_vec(self)
    }
}

/// Instruction builder for `RecoverDvp`.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` swap_dvp
///   2. `[]` nonce_tombstone
///   3. `[]` mint
///   4. `[writable]` dvp_escrow_ata
///   5. `[writable]` signer_dest_ata
///   6. `[]` token_program
///   7. `[]` memo_program
#[derive(Clone, Debug, Default)]
pub struct RecoverDvpBuilder {
    signer: Option<solana_pubkey::Pubkey>,
    swap_dvp: Option<solana_pubkey::Pubkey>,
    nonce_tombstone: Option<solana_pubkey::Pubkey>,
    mint: Option<solana_pubkey::Pubkey>,
    dvp_escrow_ata: Option<solana_pubkey::Pubkey>,
    signer_dest_ata: Option<solana_pubkey::Pubkey>,
    token_program: Option<solana_pubkey::Pubkey>,
    memo_program: Option<solana_pubkey::Pubkey>,
    settlement_authority: Option<Pubkey>,
    user_a: Option<Pubkey>,
    user_b: Option<Pubkey>,
    mint_a: Option<Pubkey>,
    mint_b: Option<Pubkey>,
    nonce: Option<u64>,
    __remaining_accounts: Vec<solana_instruction::AccountMeta>,
}

impl RecoverDvpBuilder {
    pub fn new() -> Self {
        Self::default()
    }
    /// Depositor of the leg being recovered; must equal the leg's user; receives the closed escrow's rent
    #[inline(always)]
    pub fn signer(&mut self, signer: solana_pubkey::Pubkey) -> &mut Self {
        self.signer = Some(signer);
        self
    }
    /// Closed SwapDvp address, re-derived from the seed args; must be system-owned and empty (signs the CPIs)
    #[inline(always)]
    pub fn swap_dvp(&mut self, swap_dvp: solana_pubkey::Pubkey) -> &mut Self {
        self.swap_dvp = Some(swap_dvp);
        self
    }
    /// Nonce tombstone PDA for swap_dvp; must be program-owned
    #[inline(always)]
    pub fn nonce_tombstone(&mut self, nonce_tombstone: solana_pubkey::Pubkey) -> &mut Self {
        self.nonce_tombstone = Some(nonce_tombstone);
        self
    }
    /// Mint of the leg being recovered; mint_a if signed by user_a, mint_b if by user_b
    #[inline(always)]
    pub fn mint(&mut self, mint: solana_pubkey::Pubkey) -> &mut Self {
        self.mint = Some(mint);
        self
    }
    /// Recreated escrow ATA for the closed DvP (drained, then closed)
    #[inline(always)]
    pub fn dvp_escrow_ata(&mut self, dvp_escrow_ata: solana_pubkey::Pubkey) -> &mut Self {
        self.dvp_escrow_ata = Some(dvp_escrow_ata);
        self
    }
    /// Signer's canonical ATA for the leg's mint
    #[inline(always)]
    pub fn signer_dest_ata(&mut self, signer_dest_ata: solana_pubkey::Pubkey) -> &mut Self {
        self.signer_dest_ata = Some(signer_dest_ata);
        self
    }
    /// SPL Token or Token-2022 program; must own dvp_escrow_ata (the program the escrow was created under, regardless of the mint's current owner)
    #[inline(always)]
    pub fn token_program(&mut self, token_program: solana_pubkey::Pubkey) -> &mut Self {
        self.token_program = Some(token_program);
        self
    }
    /// SPL Memo program; only used if signer_dest_ata requires a memo
    #[inline(always)]
    pub fn memo_program(&mut self, memo_program: solana_pubkey::Pubkey) -> &mut Self {
        self.memo_program = Some(memo_program);
        self
    }
    #[inline(always)]
    pub fn settlement_authority(&mut self, settlement_authority: Pubkey) -> &mut Self {
        self.settlement_authority = Some(settlement_authority);
        self
    }
    #[inline(always)]
    pub fn user_a(&mut self, user_a: Pubkey) -> &mut Self {
        self.user_a = Some(user_a);
        self
    }
    #[inline(always)]
    pub fn user_b(&mut self, user_b: Pubkey) -> &mut Self {
        self.user_b = Some(user_b);
        self
    }
    #[inline(always)]
    pub fn mint_a(&mut self, mint_a: Pubkey) -> &mut Self {
        self.mint_a = Some(mint_a);
        self
    }
    #[inline(always)]
    pub fn mint_b(&mut self, mint_b: Pubkey) -> &mut Self {
        self.mint_b = Some(mint_b);
        self
    }
    #[inline(always)]
    pub fn nonce(&mut self, nonce: u64) -> &mut Self {
        self.nonce = Some(nonce);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(&mut self, account: solana_instruction::AccountMeta) -> &mut Self {
        self.__remaining_accounts.push(account);
        self
    }
    /// Add additional accounts to the instruction.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[solana_instruction::AccountMeta],
    ) -> &mut Self {
        self.__remaining_accounts.extend_from_slice(accounts);
        self
    }
    #[allow(clippy::clone_on_copy)]
    pub fn instruction(&self) -> solana_instruction::Instruction {
        let accounts = RecoverDvp {
            signer: self.signer.expect("signer is not set"),
            swap_dvp: self.swap_dvp.expect("swap_dvp is not set"),
            nonce_tombstone: self.nonce_tombstone.expect("nonce_tombstone is not set"),
            mint: self.mint.expect("mint is not set"),
            dvp_escrow_ata: self.dvp_escrow_ata.expect("dvp_escrow_ata is not set"),
            signer_dest_ata: self.signer_dest_ata.expect("signer_dest_ata is not set"),
            token_program: self.token_program.expect("token_program is not set"),
            memo_program: self.memo_program.expect("memo_program is not set"),
        };
        let args = RecoverDvpInstructionArgs {
            settlement_authority: self
                .settlement_authority
                .clone()
                .expect("settlement_authority is not set"),
            user_a: self.user_a.clone().expect("user_a is not set"),
            user_b: self.user_b.clone().expect("user_b is not set"),
            mint_a: self.mint_a.clone().expect("mint_a is not set"),
            mint_b: self.mint_b.clone().expect("mint_b is not set"),
            nonce: self.nonce.clone().expect("nonce is not set"),
        };

        accounts.instruction_with_remaining_accounts(args, &self.__remaining_accounts)
    }
}

/// `recover_dvp` CPI accounts.
pub struct RecoverDvpCpiAccounts<'a, 'b> {
    /// Depositor of the leg being recovered; must equal the leg's user; receives the closed escrow's rent
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Closed SwapDvp address, re-derived from the seed args; must be system-owned and empty (signs the CPIs)
    pub swap_dvp: &'b solana_account_info::AccountInfo<'a>,
    /// Nonce tombstone PDA for swap_dvp; must be program-owned
    pub nonce_tombstone: &'b solana_account_info::AccountInfo<'a>,
    /// Mint of the leg being recovered; mint_a if signed by user_a, mint_b if by user_b
    pub mint: &'b solana_account_info::AccountInfo<'a>,
    /// Recreated escrow ATA for the closed DvP (drained, then closed)
    pub dvp_escrow_ata: &'b solana_account_info::AccountInfo<'a>,
    /// Signer's canonical ATA for the leg's mint
    pub signer_dest_ata: &'b solana_account_info::AccountInfo<'a>,
    /// SPL Token or Token-2022 program; must own dvp_escrow_ata (the program the escrow was created under, regardless of the mint's current owner)
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
    /// SPL Memo program; only used if signer_dest_ata requires a memo
    pub memo_program: &'b solana_account_info::AccountInfo<'a>,
}

/// `recover_dvp` CPI instruction.
pub struct RecoverDvpCpi<'a, 'b> {
    /// The program to invoke.
    pub __program: &'b solana_account_info::AccountInfo<'a>,
    /// Depositor of the leg being recovered; must equal the leg's user; receives the closed escrow's rent
    pub signer: &'b solana_account_info::AccountInfo<'a>,
    /// Closed SwapDvp address, re-derived from the seed args; must be system-owned and empty (signs the CPIs)
    pub swap_dvp: &'b solana_account_info::AccountInfo<'a>,
    /// Nonce tombstone PDA for swap_dvp; must be program-owned
    pub nonce_tombstone: &'b solana_account_info::AccountInfo<'a>,
    /// Mint of the leg being recovered; mint_a if signed by user_a, mint_b if by user_b
    pub mint: &'b solana_account_info::AccountInfo<'a>,
    /// Recreated escrow ATA for the closed DvP (drained, then closed)
    pub dvp_escrow_ata: &'b solana_account_info::AccountInfo<'a>,
    /// Signer's canonical ATA for the leg's mint
    pub signer_dest_ata: &'b solana_account_info::AccountInfo<'a>,
    /// SPL Token or Token-2022 program; must own dvp_escrow_ata (the program the escrow was created under, regardless of the mint's current owner)
    pub token_program: &'b solana_account_info::AccountInfo<'a>,
    /// SPL Memo program; only used if signer_dest_ata requires a memo
    pub memo_program: &'b solana_account_info::AccountInfo<'a>,
    /// The arguments for the instruction.
    pub __args: RecoverDvpInstructionArgs,
}

impl<'a, 'b> RecoverDvpCpi<'a, 'b> {
    pub fn new(
        program: &'b solana_account_info::AccountInfo<'a>,
        accounts: RecoverDvpCpiAccounts<'a, 'b>,
        args: RecoverDvpInstructionArgs,
    ) -> Self {
        Self {
            __program: program,
            signer: accounts.signer,
            swap_dvp: accounts.swap_dvp,
            nonce_tombstone: accounts.nonce_tombstone,
            mint: accounts.mint,
            dvp_escrow_ata: accounts.dvp_escrow_ata,
            signer_dest_ata: accounts.signer_dest_ata,
            token_program: accounts.token_program,
            memo_program: accounts.memo_program,
            __args: args,
        }
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], &[])
    }
    #[inline(always)]
    pub fn invoke_with_remaining_accounts(
        &self,
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(&[], remaining_accounts)
    }
    #[inline(always)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        self.invoke_signed_with_remaining_accounts(signers_seeds, &[])
    }
    #[allow(clippy::arithmetic_side_effects)]
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed_with_remaining_accounts(
        &self,
        signers_seeds: &[&[&[u8]]],
        remaining_accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> solana_program_error::ProgramResult {
        let mut accounts = Vec::with_capacity(8 + remaining_accounts.len());
        accounts.push(solana_instruction::AccountMeta::new(*self.signer.key, true));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.swap_dvp.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.nonce_tombstone.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.mint.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.dvp_escrow_ata.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new(
            *self.signer_dest_ata.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.token_program.key,
            false,
        ));
        accounts.push(solana_instruction::AccountMeta::new_readonly(
            *self.memo_program.key,
            false,
        ));
        remaining_accounts.iter().for_each(|remaining_account| {
            accounts.push(solana_instruction::AccountMeta {
                pubkey: *remaining_account.0.key,
                is_signer: remaining_account.2,
                is_writable: remaining_account.1,
            })
        });
        let mut data = RecoverDvpInstructionData::new().try_to_vec().unwrap();
        let mut args = self.__args.try_to_vec().unwrap();
        data.append(&mut args);

        let instruction = solana_instruction::Instruction {
            program_id: crate::DVP_SWAP_PROGRAM_ID,
            accounts,
            data,
        };
        let mut account_infos = Vec::with_capacity(9 + remaining_accounts.len());
        account_infos.push(self.__program.clone());
        account_infos.push(self.signer.clone());
        account_infos.push(self.swap_dvp.clone());
        account_infos.push(self.nonce_tombstone.clone());
        account_infos.push(self.mint.clone());
        account_infos.push(self.dvp_escrow_ata.clone());
        account_infos.push(self.signer_dest_ata.clone());
        account_infos.push(self.token_program.clone());
        account_infos.push(self.memo_program.clone());
        remaining_accounts
            .iter()
            .for_each(|remaining_account| account_infos.push(remaining_account.0.clone()));

        if signers_seeds.is_empty() {
            solana_cpi::invoke(&instruction, &account_infos)
        } else {
            solana_cpi::invoke_signed(&instruction, &account_infos, signers_seeds)
        }
    }
}

/// Instruction builder for `RecoverDvp` via CPI.
///
/// ### Accounts:
///
///   0. `[writable, signer]` signer
///   1. `[]` swap_dvp
///   2. `[]` nonce_tombstone
///   3. `[]` mint
///   4. `[writable]` dvp_escrow_ata
///   5. `[writable]` signer_dest_ata
///   6. `[]` token_program
///   7. `[]` memo_program
#[derive(Clone, Debug)]
pub struct RecoverDvpCpiBuilder<'a, 'b> {
    instruction: Box<RecoverDvpCpiBuilderInstruction<'a, 'b>>,
}

impl<'a, 'b> RecoverDvpCpiBuilder<'a, 'b> {
    pub fn new(program: &'b solana_account_info::AccountInfo<'a>) -> Self {
        let instruction = Box::new(RecoverDvpCpiBuilderInstruction {
            __program: program,
            signer: None,
            swap_dvp: None,
            nonce_tombstone: None,
            mint: None,
            dvp_escrow_ata: None,
            signer_dest_ata: None,
            token_program: None,
            memo_program: None,
            settlement_authority: None,
            user_a: None,
            user_b: None,
            mint_a: None,
            mint_b: None,
            nonce: None,
            __remaining_accounts: Vec::new(),
        });
        Self { instruction }
    }
    /// Depositor of the leg being recovered; must equal the leg's user; receives the closed escrow's rent
    #[inline(always)]
    pub fn signer(&mut self, signer: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.signer = Some(signer);
        self
    }
    /// Closed SwapDvp address, re-derived from the seed args; must be system-owned and empty (signs the CPIs)
    #[inline(always)]
    pub fn swap_dvp(&mut self, swap_dvp: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.swap_dvp = Some(swap_dvp);
        self
    }
    /// Nonce tombstone PDA for swap_dvp; must be program-owned
    #[inline(always)]
    pub fn nonce_tombstone(
        &mut self,
        nonce_tombstone: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.nonce_tombstone = Some(nonce_tombstone);
        self
    }
    /// Mint of the leg being recovered; mint_a if signed by user_a, mint_b if by user_b
    #[inline(always)]
    pub fn mint(&mut self, mint: &'b solana_account_info::AccountInfo<'a>) -> &mut Self {
        self.instruction.mint = Some(mint);
        self
    }
    /// Recreated escrow ATA for the closed DvP (drained, then closed)
    #[inline(always)]
    pub fn dvp_escrow_ata(
        &mut self,
        dvp_escrow_ata: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.dvp_escrow_ata = Some(dvp_escrow_ata);
        self
    }
    /// Signer's canonical ATA for the leg's mint
    #[inline(always)]
    pub fn signer_dest_ata(
        &mut self,
        signer_dest_ata: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.signer_dest_ata = Some(signer_dest_ata);
        self
    }
    /// SPL Token or Token-2022 program; must own dvp_escrow_ata (the program the escrow was created under, regardless of the mint's current owner)
    #[inline(always)]
    pub fn token_program(
        &mut self,
        token_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.token_program = Some(token_program);
        self
    }
    /// SPL Memo program; only used if signer_dest_ata requires a memo
    #[inline(always)]
    pub fn memo_program(
        &mut self,
        memo_program: &'b solana_account_info::AccountInfo<'a>,
    ) -> &mut Self {
        self.instruction.memo_program = Some(memo_program);
        self
    }
    #[inline(always)]
    pub fn settlement_authority(&mut self, settlement_authority: Pubkey) -> &mut Self {
        self.instruction.settlement_authority = Some(settlement_authority);
        self
    }
    #[inline(always)]
    pub fn user_a(&mut self, user_a: Pubkey) -> &mut Self {
        self.instruction.user_a = Some(user_a);
        self
    }
    #[inline(always)]
    pub fn user_b(&mut self, user_b: Pubkey) -> &mut Self {
        self.instruction.user_b = Some(user_b);
        self
    }
    #[inline(always)]
    pub fn mint_a(&mut self, mint_a: Pubkey) -> &mut Self {
        self.instruction.mint_a = Some(mint_a);
        self
    }
    #[inline(always)]
    pub fn mint_b(&mut self, mint_b: Pubkey) -> &mut Self {
        self.instruction.mint_b = Some(mint_b);
        self
    }
    #[inline(always)]
    pub fn nonce(&mut self, nonce: u64) -> &mut Self {
        self.instruction.nonce = Some(nonce);
        self
    }
    /// Add an additional account to the instruction.
    #[inline(always)]
    pub fn add_remaining_account(
        &mut self,
        account: &'b solana_account_info::AccountInfo<'a>,
        is_writable: bool,
        is_signer: bool,
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .push((account, is_writable, is_signer));
        self
    }
    /// Add additional accounts to the instruction.
    ///
    /// Each account is represented by a tuple of the `AccountInfo`, a `bool` indicating whether the account is writable or not,
    /// and a `bool` indicating whether the account is a signer or not.
    #[inline(always)]
    pub fn add_remaining_accounts(
        &mut self,
        accounts: &[(&'b solana_account_info::AccountInfo<'a>, bool, bool)],
    ) -> &mut Self {
        self.instruction
            .__remaining_accounts
            .extend_from_slice(accounts);
        self
    }
    #[inline(always)]
    pub fn invoke(&self) -> solana_program_error::ProgramResult {
        self.invoke_signed(&[])
    }
    #[allow(clippy::clone_on_copy)]
    #[allow(clippy::vec_init_then_push)]
    pub fn invoke_signed(&self, signers_seeds: &[&[&[u8]]]) -> solana_program_error::ProgramResult {
        let args = RecoverDvpInstructionArgs {
            settlement_authority: self
                .instruction
                .settlement_authority
                .clone()
                .expect("settlement_authority is not set"),
            user_a: self.instruction.user_a.clone().expect("user_a is not set"),
            user_b: self.instruction.user_b.clone().expect("user_b is not set"),
            mint_a: self.instruction.mint_a.clone().expect("mint_a is not set"),
            mint_b: self.instruction.mint_b.clone().expect("mint_b is not set"),
            nonce: self.instruction.nonce.clone().expect("nonce is not set"),
        };
        let instruction = RecoverDvpCpi {
            __program: self.instruction.__program,

            signer: self.instruction.signer.expect("signer is not set"),

            swap_dvp: self.instruction.swap_dvp.expect("swap_dvp is not set"),

            nonce_tombstone: self
                .instruction
                .nonce_tombstone
                .expect("nonce_tombstone is not set"),

            mint: self.instruction.mint.expect("mint is not set"),

            dvp_escrow_ata: self
                .instruction
                .dvp_escrow_ata
                .expect("dvp_escrow_ata is not set"),

            signer_dest_ata: self
                .instruction
                .signer_dest_ata
                .expect("signer_dest_ata is not set"),

            token_program: self
                .instruction
                .token_program
                .expect("token_program is not set"),

            memo_program: self
                .instruction
                .memo_program
                .expect("memo_program is not set"),
            __args: args,
        };
        instruction.invoke_signed_with_remaining_accounts(
            signers_seeds,
            &self.instruction.__remaining_accounts,
        )
    }
}

#[derive(Clone, Debug)]
struct RecoverDvpCpiBuilderInstruction<'a, 'b> {
    __program: &'b solana_account_info::AccountInfo<'a>,
    signer: Option<&'b solana_account_info::AccountInfo<'a>>,
    swap_dvp: Option<&'b solana_account_info::AccountInfo<'a>>,
    nonce_tombstone: Option<&'b solana_account_info::AccountInfo<'a>>,
    mint: Option<&'b solana_account_info::AccountInfo<'a>>,
    dvp_escrow_ata: Option<&'b solana_account_info::AccountInfo<'a>>,
    signer_dest_ata: Option<&'b solana_account_info::AccountInfo<'a>>,
    token_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    memo_program: Option<&'b solana_account_info::AccountInfo<'a>>,
    settlement_authority: Option<Pubkey>,
    user_a: Option<Pubkey>,
    user_b: Option<Pubkey>,
    mint_a: Option<Pubkey>,
    mint_b: Option<Pubkey>,
    nonce: Option<u64>,
    /// Additional instruction accounts `(AccountInfo, is_writable, is_signer)`.
    __remaining_accounts: Vec<(&'b solana_account_info::AccountInfo<'a>, bool, bool)>,
}
